services:
  workspace:
    gpus: all
    devices:
      - /dev/bus/usb:/dev/bus/usb
    environment:
      DISPLAY: ${DISPLAY:-:0}
      QT_X11_NO_MITSHM: "1"
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
